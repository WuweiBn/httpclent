<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

<properties></properties>
		
		<settings>
		<!-- 显示指定得每一个我们需要配置得值，即使它是默认得。防止版本更新带来得问题 -->
			<setting name="lazyLoadingEnabled" value="true"/>
			<setting name="aggressiveLazyLoading" value="false"/>
		</settings>
<!--  
	1.mybatis 可以使用properties 来引入外部的properties配置文件内容；
		resource:引入类路径下的资源
		url:引入网络路劲或磁盘路径下的资源
		<properties></properties>
-->

<!-- 
	2.settings包含很多重要的设置项
		setting:用来设置每一个设置项
		name:设置项名
		value:设置取值 
 <settings>
 <setting name="" value=""/>
 </settings>
 -->

<!-- 
<typeAliases>	</typeAliases>
	3.typeAliases:别名处理器:可以为我们的java类型起别名   别名不分大小写
		1.typeAlias:为某个java类型起别名
			type:指定要起别名类型全类名，默认别名就是类名小写
			alias:指定新的别名
			 	<typeAlias type="" alias=""/>
		2.package:为某个包下的所有类批量起别名
			name:指定包名（为当前包以及下面所有后代包的每一个类都起一个默认别名（类名小写））
				<package name=""/>
		3.批量起别名的情况下，使用@Alias注解为为某个类型指定一个新的别名
 -->
 
 <!-- 
 	4.environments:环境们，mybatis可以配置多种环境，default:指定使用某种环境，可以达到快速切换环境。
 		environment:配置一个具体的环境信息，必须有2个标签， id:代表当前环境的唯一标识
 			transactionManager :事物管理器
 			dataSource:事物管理器的类型
  -->
 	
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="com.mysql.jdbc.Driver" />
				<property name="url" value="jdbc:mysql://rm-2ze0q80w59h4uyvx4rw.mysql.rds.aliyuncs.com:3306/xin_finance_kits" />
				<property name="username" value="xin" />
				<property name="password" value="48sdf37EB7" />
			</dataSource>
		</environment>
	</environments>
	<!-- 
	将我们写好的sql一定要注册到全局文件中
	 -->
	 
<!--
	 mapper注册一个sql映射
	 resource：引用类路径下的映射文件
	 url:引用网络路径或者磁盘路径下的sql映射文件
	 class:引用接口，
	 	1.有sql映射文件，映射文件名必须与接口同名，并且放在接口同一目录下
	 	2.没有sql映射文件，所有sql都是利用注释写在接口上
 -->
	<mappers>
		<!-- <mapper resource="EmployeeMapper.xml" /> -->
		<package name="com.unxin"/>

	</mappers>
</configuration>

